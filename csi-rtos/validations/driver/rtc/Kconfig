choice
       prompt "select"
       depends on BUILD_VAL_RTC
       default BUILD_VAL_RTC01
       help
         select the test module

config BUILD_VAL_RTC01
       bool "build validation rtc01"
       depends on BUILD_VAL_RTC
       help
         select the rtc driver validation

config BUILD_VAL_RTC02
       bool "build validation rtc02"
       depends on BUILD_VAL_RTC
       help
         select the rtc driver validation
endchoice
