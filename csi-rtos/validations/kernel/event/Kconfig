choice
       prompt "select"
       depends on BUILD_VAL_EVENT
       default BUILD_VAL_EVENT01
       help
         select the test module

config BUILD_VAL_EVENT01
       bool "build validation event01"
       depends on BUILD_VAL_EVENT
       help
         select the event test

endchoice
